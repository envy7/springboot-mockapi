version: "3.9"

services:
  mockapi:
    build: ../../server
    ports:
      - "8080:8080"  # Map container port 8080 to host port 8080
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      start_period: 30s
      interval: 5s
      timeout: 5s
      retries: 300